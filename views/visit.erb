<h2>Записаться</h2>

<p>Здесь вы всегда можете записаться к нам в Barber Shop</p>

<script type="text/javascript">
   
   //alert('Hello from visit page!'); // заявить JS только на выбранной странице

   $(function() {
         // Тут я могу написать любой js код который выполнится после загрузки
         // страницы, много строк
      $('#color').simplecolorpicker();  // вызов плагина на странице
      $('#datetimepicker').datetimepicker();
   });
   

</script>

<form action="/visit" method="POST" role="form">
  
   <div class="form-group">
    <label class="form-label">Ваше имя</label>
    <input name="username" type="text" class="form-control" placeholder="Ваше имя" value="<%= @username %>"> 
    <!-- с помощью конкатенации через параметр value и переменную оставляем ввод пользователя если ошибка на других полях. но можно так делать еще чище (позже). -->
   </div>

	<div class="form-group">
    <label class="form-label">Ваш телефон</label>
    <input name="phone" type="text" class="form-control" placeholder="Ваш телефон" value="<%= @phone %>">
   </div>

   <div class="form-group">
    <label class="form-label">Когда Вас ждать?</label>
    <input id="datetimepicker" name="datetime" type="text" class="form-control" placeholder="Введите время и дату" value="<%= @datetime %>">
   </div>

   <div class="form-group">
      <label>Ваш мастер?!</label>
      <select name="barber" class="form-control">

         <!--  Для выбора и сохранения его в форме при незаполненных других полях используется <%= %> оборот Ruby для вставки выполняемого правила -->
        <option <%= @barber == 'Jassie Pinkman' ? 'selected' : '' %>>Jassie Pinkman</option>
        <option <%= @barber == 'Tony Walkins' ? 'selected' : '' %>>Tony Walkins</option>
        <option <%= @barber == 'Bruce Wane' ? 'selected' : '' %>>Bruce Wane</option>
      </select>
   </div>


   <div class="form-group">
      <label class="form-label">Выберите цвет</label>
      <select id="color" name="color">
           <option value="#7bd148">Green</option>
           <option value="#5484ed">Bold blue</option>
           <option value="#a4bdfc">Blue</option>
           <option value="#46d6db">Turquoise</option>
           <option value="#7ae7bf">Light green</option>
           <option value="#51b749">Bold green</option>
           <option value="#fbd75b">Yellow</option>
           <option value="#ffb878">Orange</option>
           <option value="#ff887c">Red</option>
           <option value="#dc2127">Bold red</option>
           <option value="#dbadff">Purple</option>
           <option value="#e1e1e1">Gray</option>
      </select>
   </div>
   
  
  <button type="submit" class="btn btn-primary">Записаться</button>
</form>

